import React from "react";

const Titulo = ({text}) => {
    return(
        <div className="titulo-contain">
           <label className="titulo-label"> {text} </label>
        </div>
     )
};
export default Titulo;
